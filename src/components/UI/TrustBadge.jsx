export const TrustBadge = ({ label }) => {
  return (
    <span className="rounded-full border border-gold/30 bg-navy/70 px-4 py-2 text-xs font-semibold uppercase tracking-wide text-gold">
      {label}
    </span>
  );
};
